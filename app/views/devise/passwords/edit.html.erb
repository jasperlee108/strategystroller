<div class="logincontainer" style="background-color: #f5f5f5">
<div class="form-signin">
<h2 class="form-signin-heading"><%= t :change_password %></h2>

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f| %>
  <% if devise_error_messages! != "" %>
  <div class="alert"><%= devise_error_messages! %></div>
  <% end %>

  <%= f.hidden_field :reset_password_token %>

  <%= f.password_field :password, :autofocus => true, :class => "input-block-level", :placeholder =>(t :new_password) %>

  <%= f.password_field :password_confirmation :class => "input-block-level", :placeholder =>(t :confirm_new_password) %>

  	<div class="row-fluid"><%= f.submit (t :change_my_password), :class=> "btn btn-large btn-primary span12" %>
   <input type=button class="btn btn-large btn-primary span12" value= "<%= t :cancel%>" onclick= "cancel_button()">
   </div>
<% end %>

<%= render "devise/shared/links" %>
