<!-- views/devise/menu/_login_items.html.erb -->
<% if user_signed_in? %>
    <li>
      <%= link_to((t '.logout'), destroy_user_session_path, :method => :delete) %>
    </li>
<% else %>
    <li>
      <%= link_to((t '.login'), new_user_session_path)  %>
    </li>
    <li>
      <%= link_to((t '.admin'), new_admin_user_session_path)  %>
    </li>
<% end %>