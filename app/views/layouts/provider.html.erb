<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Strategy Stroller</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <%= stylesheet_link_tag    "application", :media => "all" %>



    <%= csrf_meta_tags %>

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="../assets/js/html5shiv.js"></script>
    <![endif]-->
  </head>

  <body>



    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="/">Strategy Stroller</a>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <%= render 'devise/menu/login_items' %>
              <%= render 'devise/menu/registration_items' %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>


 <div class="container-fluid">
      <div class="row-fluid">
        <div class="span3">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li class="nav-header">Navigation</li>
              <li id="home" class="active"><a href="/home/index">Home</a></li>
              <li><a href="/admin/login">Admin</a></li>
              <li id="define_page"><a href="<%= url_for(:action => 'define_page', :controller => 'provider') %>">Define</a></li>
              <li id="update_page"><a href="<%= url_for(:action => 'update_page', :controller => 'provider') %>">Update</a></li>
              <li class="nav-header">Context Menu</li>
              <%= yield :context_menu %>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="span9">
          <div class="hero-unit">
            <p class="notice"><%= notice %></p>
            <p class="alert"><%= alert %></p>
            <%= yield %>
          </div>
        </div><!--/span-->
      </div><!--/row-->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <!-- Added for cocoon gem -->
    <%= javascript_include_tag :cocoon %>
  </body>
</html>

