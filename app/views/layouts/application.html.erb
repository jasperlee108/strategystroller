<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<title><%= t '.strategy' %></title>
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<meta name="description" content="">
		<meta name="author" content="">

		<%= stylesheet_link_tag    "application", :media => "all" %>

		<%= javascript_include_tag "application" %>
		<!-- Only use application for all js files or individual calls for each to prevent duplication
		<%= javascript_include_tag "custom" %>-->
		<!-- Added for cocoon gem -->
		<!--<%= javascript_include_tag :cocoon %>-->
		<!--<link rel="stylesheet" href="http://code.jquery.com/ui/1.10.2/themes/smoothness/jquery-ui.css" />
		<script src="http://code.jquery.com/jquery-1.9.1.js"></script>
		<script src="http://code.jquery.com/ui/1.10.2/jquery-ui.js"></script>
		<script src="/assets/twitter/bootstrap/bootstrap-dropdown.js">-->

		<%= csrf_meta_tags %>

		<!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
		<!--[if lt IE 9]>
		<script src="../assets/js/html5shiv.js"></script>
		<![endif]-->
	</head>

	<body>

		<div class="navbar navbar-inverse navbar-fixed-top mainnav">

			<div class="navbar-inner">
				<div class="container">
					<a class="brand" href="/"><%= t '.strategy_1' %></a>
					<% if signed_in? %>
					<% if current_user.respond_to?('controlling_unit') %>
					<% if current_user.controlling_unit == true %>
					<!-- <div id ="cu_nav">-->
					<ul class = "nav">
						
						<% if current_page? (setup_system_path) %>
							<li id= "navbar_setup" class="active">
						<% else %>
							<li id= "navbar_setup">
						<% end %>
							<%= link_to (t :setup), setup_system_path %>
						</li>
						
						<% if current_page? (create_users_path) %>
							<li id= "navbar_create_users" class="active">
						<% else %>
							<li id= "navbar_create_users">
						<% end %>
							<%= link_to (t :create_users), create_users_path %>
						</li>
					
						<% if current_page? (all_form_path) or current_page? (goals_path) or current_page? (indicators_path) or current_page? (projects_path)%>
							<li id ="navbar_framework" class="active">
						<% else %>
							<li id ="navbar_framework">
						<% end %>
							<%= link_to (t :framework), all_form_path %>
						</li>
						
						<% if current_page? (cu_review_path) %>
							<li id ="navbar_review" class="active">
						<% else %>
							<li id ="navbar_review">
						<% end %>
							<%= link_to (t :review), cu_review_path %>
						</li>
						
						<% if current_page? (all_data_path) or current_page? (graph_panel_path) %>
							<li id="navbar_report" class="active">
						<% else %>
							<li id="navbar_report">
						<% end %>
							<%= link_to (t :report), all_data_path %>
						</li>
						
					</ul>

					<!--     </div>-->
					<% end %>
					<% end %>

					<div class="btn-group pull-right right_navbar_drop">
						<a class="btn dropdown-toggle btn-inverse pull-right" data-toggle="dropdown"> <%= t '.user_settings' %> <span class="caret"></span> </a>
						<ul class="dropdown-menu">
							<li>
								<%= render 'devise/menu/login_items' %>
							</li>
							<li>
								<%= render 'devise/menu/registration_items' %>
							</li>
						</ul>
					</div>
					<% end %>

				</div>

			</div>
		</div>

		<div class="container body_container">
			<!-- Display messages For devise. -->
			<!-- <% if notice %>
			<div class="notice alert">
			<button type="button" class="close" data-dismiss="alert"><%= t 'times' %></button>
			<%= notice %>
			</div>

			<% end %>

			<% if alert %>
			<div class="alert">
			<button type="button" class="close" data-dismiss="alert"><%= t 'times' %></button>
			<%= alert %>
			</div>
			<% end %>-->
			<%= yield %>

		</div>
		<!-- /container -->

		<!--  <div class="container">
		<div class="footer">
		<center><%=t :footer_string%></center>
		</div>
		</div>-->
	</body>
</html>

