<h1> <%= t '.form_template' %> </h1>
<body>
  
    <%= semantic_form_for @form do |f| %>
        <%= f.inputs do %>
     
          <%= f.input :goal_id, :label => (t ".select_a_form") :as => :select, :collection => Form.all, :prompt => true, :required => true %>
          <!--<%= f.input :indicator_id, :label => (t ".pick_an_indicator"), :as => :select, :collection => Indicator.all, :prompt => true, :required => true %>-->
          <!--<%= f.input :project_id, :label => (t ".pick_a_project"), :as => :select, :collection => Project.all, :prompt => true, :required => true %>--> 

          <!--<%= f.input :user_ids, :as => :select, :size => 25, :multiple => true, :collection => User.all, :selected => User.first, :include_hidden => false, :label => (t ".select_providers_to_send") %>-->

        <% end %>
    <%= f.action :submit, :label => (t ".submit_form_to_project_owners")  %>
    <% end %>
</body>
